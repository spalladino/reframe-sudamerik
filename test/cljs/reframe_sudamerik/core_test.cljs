(ns reframe-sudamerik.core-test
  (:require
   [cljs.test :refer-macros [deftest testing is]]
   [reframe-sudamerik.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
